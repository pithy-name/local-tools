# redact.py dependencies
# Install with: pip install -r requirements.txt
# (setup.sh does this automatically inside a virtualenv)

# ── NLP / PII detection ───────────────────────────────────────────────────────
presidio-analyzer>=2.2
presidio-anonymizer>=2.2
spacy>=3.7
click>=8.0            # the spaCy CLI runs `download` via typer→click; not pulled in transitively here

# ── PDF processing ────────────────────────────────────────────────────────────
pymupdf>=1.24

# ── Image processing ──────────────────────────────────────────────────────────
Pillow>=10.0

# ── HTML parsing ──────────────────────────────────────────────────────────────
beautifulsoup4>=4.12

# ── Config ────────────────────────────────────────────────────────────────────
PyYAML>=6.0

# ── Apple Vision OCR (macOS only, on-device — strongly recommended) ───────────
# Uses your M-series Neural Engine. No data leaves the machine.
pyobjc-core>=10.0
pyobjc-framework-Vision>=10.0
pyobjc-framework-Quartz>=10.0

# ── Tesseract fallback (optional) ────────────────────────────────────────────
# Uncomment if you also want Tesseract as a fallback OCR engine.
# Also run: brew install tesseract
# pytesseract>=0.3.10
