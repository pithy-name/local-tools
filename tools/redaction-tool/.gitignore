# Your real config with actual redaction terms (PII) — never publish.
# Copy demo.config.yaml -> config.yaml and edit.
config.yaml

# Local design-decision record — a working doc, not a shipped artifact.
decisions.md

# Names input + derived files for gen_keywords.py (real names → pseudonyms) — PII, never publish.
names.md
names-dupes.txt

# Backups + atomic-write temps (config.yaml.bak from --write, names.md.bak, *.tmp) — may hold PII.
*.bak
*.tmp

# Opt-in --report output — itemizes matched PII, never publish.
redaction-report*.md
